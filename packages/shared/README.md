markdown
# Shared UI Kit

Переиспользуемые UI компоненты и дизайн-система для монорепозитория портфолио.

## 🚀 Быстрый старт

```
// Базовый импорт
import { Button, Card, Typography, Tabs, CustomLink } from "@packages/shared/src";

// Или прямой импорт
import { Button } from "@packages/shared/src/components/ui/Button";
```

## Доступные компоненты

### Базовые компоненты

Button - варианты primary | secondary | outline | disabled

Card - контейнер с вариантами отступов (sm | md | lg)

Input - стилизованное поле ввода

Typography - типографика с вариантами (h1-h4 | p | small | lead | muted | code)

Tabs - вкладки с поддержкой options и contentOptions

CustomLink - ссылки с вариантами (default | primary | secondary | muted) и поддержкой внешних ссылок

ThemeToggle - кнопка переключения темы


### Компоненты Layout

Container - Центрированный контейнер с максимальной шириной

Section - Секция с вариантами фона

Grid && GridItem - Адаптивная grid-система

### Утилиты для разработки
CodeBlock - подсветка синтаксиса (TypeScript, JSX, TSX)

CodePreview - live превью кода с переключением между кодом и демо

PreviewPage - полная демонстрация всех компонентов



# Тема

ThemeProvider - Провайдер контекста для управления темой

useTheme - Хук для доступа к контексту темы

ThemeToggle - Кнопка переключения тем

## Система тем
```
 Светлая тема
Акцент: #A6FF00 (зеленый)

Фон: #FFFFFF

Серый: #C2C2C2

Темная тема
Акцент: #FFA200 (оранжевый)

Фон: #111111

Серый: #8B0D98 (фиолетовый)
```

# Использование

```
import { ThemeProvider, useTheme } from "@packages/shared";

const App = () => (
  <ThemeProvider>
    <YourApp />
  </ThemeProvider>
);

const Component = () => {
  const { theme, toggle, setTheme } = useTheme();
  return <button onClick={toggle}>Переключить тему</button>;
};
```






## Структура проекта

```
shared/
├── src/
│   ├── components/
│   │   ├── icons/      # Компоненты иконок
│   │   ├── layout/     # Компоненты layout (Container, Section, Grid)
│   │   └── ui/         # UI компоненты (Button, Card, Typography, Tabs, CustomLink)
│   ├── hooks/      # Кастомные React хуки
│   ├── providers/  # Провайдеры контекста (ThemeProvider)
│   ├── routes/     # Конфигурации маршрутов
│   ├── tailwind/   # Конфигурация Tailwind
│   ├── utils/      # Утилиты (cn)
│   └── index.ts        # Основные экспорты
├── package.json
├── tailwind.config.js  # Конфигурация Tailwind
├── README.md           # документация UI-Kit
└── tsconfig.json       # Конфигурация TypeScript

```

## Превью компонентов

Запустите dev server:

```
bash
cd services/host
npm start
```

Откройте http://localhost:3000/customStoryBook чтобы увидеть все компоненты в действии.

## Разработка

### Рекомендации

Делайте компоненты маленькими и презентационными

Используйте TypeScript для всех компонентов

Следуйте токенам дизайн-системы

Поддерживайте светлую и темную темы

Делайте компоненты адаптивными по умолчанию

Используйте утилиту cn() для объединения классов

### Шрифты
Система использует:

Rubik Burned - для заголовков H1

Science Gothic - для подзаголовков и акцентного текста

Inter - для основного текста (fallback)

### 📖 Справка API
Подробную документацию по пропсам каждого компонента можно увидеть во вкладке "Props" на странице превью или в исходном коде компонентов.