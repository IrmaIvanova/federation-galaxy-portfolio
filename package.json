{
  "name": "portfolio",
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
    "scripts": {
    "start": "npm-run-all --parallel start:*",
    "start:host": "cd services/host && npm start",
    "start:portfolio": "cd services/portfolio && npm start", 
    "start:examples": "cd services/examples && npm start",
    "dev": "concurrently \"npm run start:host\" \"npm run start:portfolio\" \"npm run start:examples\"",
    "install:all": "npm install && cd services/host && npm install && cd ../portfolio && npm install && cd ../examples && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "npm-run-all": "^4.1.5"
  }
}
