// 1️⃣ React Lifecycle (классовые и функциональные компоненты)

// Классические каверзные вопросы:

// В каком порядке вызываются методы жизненного цикла в классовом компоненте?

// constructor → render → componentDidMount → componentDidUpdate → componentWillUnmount
// ⚡ Подводный камень: componentDidUpdate вызывается после любого обновления, включая state/props.

// Почему нельзя вызывать setState в render?

// Это создаст бесконечный ререндер.
// ⚡ Подводный камень: иногда пытаются сделать вычисления в render и сразу обновить state → ловушка.

// Разница между useEffect и useLayoutEffect?

// useEffect – асинхронно после рендера, не блокирует экран.

// useLayoutEffect – синхронно после DOM-обновления, до отображения на экране.
// ⚡ Подводный камень: если забыть зависимость в useEffect, будет stale closure (старые значения state/props).

// Что такое «чистка эффекта» (cleanup function) в useEffect?

// Функция, которая возвращается из useEffect, вызывается перед размонтированием или перед следующим вызовом эффекта.
// ⚡ Подводный камень: можно потерять подписки или таймеры, если забыть clean-up.

// Когда useMemo/useCallback реально помогают?

// Только если есть дорогие вычисления или мемоизация колбэков, которые зависят от частых ререндеров.
// ⚡ Подводный камень: мемоизация сама по себе не ускоряет компонент, если вычисления легкие.

// Почему componentWillMount больше не используют?

// Он устарел из-за асинхронного рендера в React 18 (Concurrent Mode).

// 2️⃣ JS Lifecycle / Event Loop / Async

// Классические каверзные вопросы:

// Разница между microtasks и macrotasks

// Microtasks: Promise.then/catch/finally, queueMicrotask

// Macrotasks: setTimeout, setInterval, setImmediate
// ⚡ Подводный камень: await всегда ставит продолжение в microtask, а не в setTimeout.

// Что произойдет, если вызвать setTimeout(fn, 0)?

// fn будет поставлен в очередь macrotask, выполнится после всех microtasks, даже если delay=0.

// Почему async/await иногда ломает логику цикла?

// await «разбивает» функцию на два этапа: до await — синхронно, после await — как microtask.
// ⚡ Подводный камень: console.log порядок сильно отличается от того, что кажется.

// Что такое «closure over stale state»?

// Если у тебя функция захватила старое значение переменной через замыкание, новые обновления state будут игнорироваться.
// ⚡ Часто ловят в setTimeout, useEffect, event listeners.

// Почему нельзя напрямую мутировать state или объект, который зависит от render?

// Из-за того, что React/JS движок не отслеживает прямые мутации, и ререндер не произойдет.

// Если хочешь, я могу составить мини-таблицу “вопрос → ловушка → короткий ответ” именно для собеса с lifecycle — прям как шпаргалку для техлида.

// Хочешь, чтобы я так сделал?

// ChatGPT может допускать ошибки. Проверяйте важную информацию. Смотрите настройк